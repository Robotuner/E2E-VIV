<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
          xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
          xmlns:local="clr-namespace:OneVote.ViewModels"  
          xmlns:model="clr-namespace:OneVote.Models" 
          x:Class="OneVote.Views.CategoryTypeTemplate">
    <ContentView.Content>
        <Grid Padding="10" x:DataType="model:CategoryTypeItem">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Grid.GestureRecognizers>
                <TapGestureRecognizer NumberOfTapsRequired="1"
                      Command="{Binding Source={RelativeSource AncestorType={x:Type local:ItemsViewModel}}, Path=ItemTapped}"		
                          CommandParameter="{Binding .}">
                </TapGestureRecognizer>
            </Grid.GestureRecognizers>
            <StackLayout Orientation="Vertical" Grid.Row="0" Grid.Column="0">
                <Label Text="{Binding Text}" LineBreakMode="NoWrap" FontSize="16" />
                <Label Text="{Binding Description}"  LineBreakMode="NoWrap" FontSize="13" />
            </StackLayout>
            <StackLayout Orientation="Horizontal" Grid.Row="0" Grid.Column="1">
                <Label Text="{Binding Selected}" FontSize="18" VerticalOptions="Center" HorizontalOptions="Center" TextColor="DarkGray"/>
                <Label Text="/" FontSize="18" VerticalOptions="Center" HorizontalOptions="Center"/>
                <Label Text="{Binding Total}" FontSize="18" VerticalOptions="Center" HorizontalOptions="Center" TextColor="DarkGray"/>
            </StackLayout>
            <BoxView Grid.Row="1" Grid.ColumnSpan="2" HeightRequest="1" BackgroundColor="Black"/>
        </Grid>
    </ContentView.Content>
</ContentView>